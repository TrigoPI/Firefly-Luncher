import { SERVER_STATE } from "./ServerState";
import { EventHandler } from "../lib/event/Event";
export default class McServer {
    private logger;
    private name;
    private version;
    private command;
    private buffer;
    private state;
    private eventListener;
    private serverExe;
    private xmx;
    private xms;
    private customStartCommand;
    private serverProperties;
    constructor(path: string, name: string, version: string);
    GetServerProperties(): Record<string, string>;
    GetName(): string;
    GetVersion(): string;
    GetState(): SERVER_STATE;
    GetBuffer(): string[];
    IsStopped(): boolean;
    IsLoading(): boolean;
    IsRunning(): boolean;
    SetXmx(xmx: number): void;
    SetXms(xms: number): void;
    SetServerExe(name: string): void;
    SetCustomStartCommand(cmd: string): void;
    AddEventListener(listener: EventHandler): void;
    Write(cmd: string): void;
    Stop(): void;
    Start(): void;
    private ChangeState;
    private OnEvent;
    private OnCmdOutEvent;
    private OnCmdExitEvent;
    private OnCmdErrorEvent;
    private ReadServerProperties;
}
